<div class="apprenant-index">
  <div class="apprenant-table-container">
    <div class="apprenant-table-container-scrollable">
      <table>
        <thead>
          <th>Nom</th>
          <th>Prénom</th>
          <th>Ville</th>
          <th>Tarif</th>
        </thead>
        <tbody>
          <% Intervenant.all.order(:nom).each do |intervenant| %>
              <tr data-link="<%= intervenant_path(intervenant)%>" data-mesdonnees-search-bar-target="row">
                <td><%= intervenant.nom %></td>
                <td><%= intervenant.prenom %></td>
                <td><%= intervenant.ville %></td>
                <td><%= intervenant.tarif %>€</td>
              </tr>
          <% end %>
        </tbody>
      </table>
  </div>
  </div>
</div>
